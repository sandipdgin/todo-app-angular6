import { TodoListDirective } from './todo-list.directive';

describe('TodoListDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoListDirective();
    expect(directive).toBeTruthy();
  });
});
